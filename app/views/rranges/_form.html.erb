<%= form_for(rrange) do |f| %>
  <% if rrange.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rrange.errors.count, "error") %> prohibited this rrange from being saved:</h2>

      <ul>
      <% rrange.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :zone_id %>
    <%= f.text_field :zone_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
